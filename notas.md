DB_HOST     → localhost
DB_PORT     → 5432 (casi siempre)
DB_USER     → el usuario que usás para conectarte (normalmente "postgres")
DB_PASSWORD → la contraseña que pusiste al instalar PostgreSQL
DB_NAME     → el nombre de tu base de datos (la que ya tenías)